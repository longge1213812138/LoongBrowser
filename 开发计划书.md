# 极简 Chromium 内核浏览器「LoongBrowser」开发计划书

> 版本：v1.1 ｜ 日期：2026-09-14 ｜ 项目目录：`C:\Users\91533\Desktop\浏览器`
> v1.1 变更（按用户确认）：① 浏览器命名 **LoongBrowser**；② 默认主页改为**空白页（about:blank）**；③ **安装位置支持自定义**。

---

## 一、项目目标

开发一款基于 Chrome 内核（Chromium）的 Windows 桌面浏览器，功能极简、依赖极少，最终交付**可安装的 EXE 安装程序**。

### 核心需求
1. 基础网页浏览（地址栏、前进/后退/刷新、多标签页，默认主页为空白页）
2. 书签管理（添加、查看、打开、删除，本地持久化）
3. 下载管理（下载列表、保存到系统"下载"文件夹、打开/删除记录）
4. 缓存清理（一键清除缓存、历史记录、Cookie）
5. 可设置为 Windows 默认浏览器（注册 ProgId + 引导用户在系统设置中确认）

### 约束
- 尽可能减少第三方依赖
- 代码从简，单一语言实现，不引入重型框架

---

## 二、技术方案（选型与理由）

### 2.1 内核：Microsoft Edge WebView2（即 Chromium 内核）

| 方案 | 结论 | 理由 |
|---|---|---|
| WebView2 ✅ | **采用** | 微软官方 Chromium 内核控件，Windows 10 2004+ / Windows 11 **系统自带运行时**，无需打包上百 MB 的浏览器内核；仅需一个约 1MB 的官方加载器 DLL |
| CEF ❌ | 不采用 | 需捆绑完整 Chromium（100MB+），构建链复杂，违背"依赖最少"约束 |
| Chromium 源码 ❌ | 不采用 | 编译需数十 GB 工具链与数小时，完全不现实 |

> 说明：WebView2 的渲染内核与 Chrome 同源（Blink + V8），网页兼容性一致。本机已确认装有 WebView2 运行时 v152.0.4191.66。若目标机器缺失，程序启动时会提示并引导安装微软官方 Evergreen 运行时。

### 2.2 应用框架：C# + WinForms（.NET Framework 4.8）

- WinForms 是 Windows 系统自带框架，**零额外运行时依赖**（Win10/11 均内置 .NET Framework 4.x）
- 本机已验证：系统自带 `csc.exe` 编译器可直接编译，无需安装 Visual Studio
- 相比 C++/Win32：代码量少一半以上，稳定性高，符合"代码从简"

### 2.3 编译工具链（本机现状决定）

| 工具 | 状态 | 用途 |
|---|---|---|
| `csc.exe`（.NET Framework 自带） | ✅ 已有 | 编译浏览器主程序与安装程序 |
| WebView2 SDK（微软官方 NuGet 包） | ✅ 已下载（约 1MB） | 提供 WebView2 的 .NET 封装（3 个 DLL 已提取至 `libs\`） |
| 自研 C# 安装器 | ✅ 自研 | 替代 Inno Setup，不引入第三方打包工具 |

### 2.4 数据存储
- 书签 / 下载记录：JSON 文件，存于 `%APPDATA%\LoongBrowser\`
- 浏览器缓存 / Cookie / 历史：由 WebView2 用户数据目录管理（`%LOCALAPPDATA%\LoongBrowser\WebView2`），清理时调用其官方 API

---

## 三、模块划分

```
LoongBrowser（主程序，命名空间 LoongBrowser）
├── Program.cs        程序入口、命令行 URL 接收
├── MainForm.cs       主窗口：标签栏、工具栏、地址栏、菜单
├── TabManager.cs     多标签页管理（新建/切换/关闭/标题联动）
├── BookmarkStore.cs  书签模块（增删查、JSON 持久化、管理对话框）
├── DownloadStore.cs  下载模块（监听下载、记录持久化、打开文件）
├── BrowserCleaner.cs 缓存清理模块（调用 WebView2 ClearBrowsingData API）
├── DefaultBrowser.cs 默认浏览器模块（注册表注册 + 引导系统设置）
├── JsonStore.cs      极简 JSON 读写工具
└── Installer.cs      安装器（独立编译为 LoongBrowserSetup.exe）
```

### 各模块职责

| 模块 | 功能点 |
|---|---|
| MainForm | 工具栏（后退/前进/刷新/新建标签/关闭标签/收藏）、地址栏（输入 URL 或关键词回车跳转）、菜单（书签/下载/清理/设为默认浏览器/关于）。无主页按钮（默认主页为空白页） |
| TabManager | 标签栏 UI、WebView2 实例与标签一一对应、关闭标签回收资源、新标签默认打开 about:blank；**新窗口请求分流：同站跳转留在当前标签（写入历史，前进/后退可用），跨站链接才新开标签页**（按主域判定，忽略子域差异）；**自定义右键菜单**：新标签页打开链接 / 新窗口打开链接（进程内多窗口，共享内核环境）/ 复制链接地址 / 打开图片；可编辑框保留默认菜单 |
| BookmarkStore | "收藏本页"、书签管理器列表（双击打开、按钮删除）、`%APPDATA%\LoongBrowser\bookmarks.json` |
| DownloadStore | 拦截 WebView2 下载事件→自动保存到系统下载文件夹→加入下载列表；支持"打开文件""打开所在文件夹""删除记录"；`downloads.json` |
| BrowserCleaner | 一键清理：HTTP 缓存、历史记录、Cookie（分开提供，避免误清登录态） |
| DefaultBrowser | ① 写入 `HKCU\Software\Clients\StartMenuInternet\LoongBrowser`（含 Capabilities/URLAssociations，声明 http/https 处理）② 注册 ProgId `LoongBrowser.URL`（shell open 命令带 `"%1"` 接收 URL）③ 自动打开 Windows"默认应用"设置页，由用户一键确认（Win10+ 系统安全机制要求，任何浏览器都无法绕过） |
| 安装器 Installer | 自研：安装位置**默认 `%LocalAppData%\Programs\LoongBrowser` 且可在界面中浏览修改**、写注册表（ProgId/默认浏览器注册/卸载信息）、创建开始菜单与桌面快捷方式、支持 `/uninstall` 完整卸载、检测 WebView2 运行时 |

> 命令行约定：主程序支持 `LoongBrowser.exe "https://..."`，点击系统内链接时由默认浏览器机制传入 URL。

---

## 四、实现步骤

| 步骤 | 内容 | 验收标准 |
|---|---|---|
| 1 | 下载 WebView2 官方 NuGet 包，提取托管 DLL 与 WebView2Loader.dll | ✅ 已完成 |
| 2 | 编写 JsonStore / BookmarkStore / DownloadStore / DefaultBrowser / BrowserCleaner | 语法编译通过 |
| 3 | 编写 MainForm / TabManager / Program（UI 与浏览主流程） | 编译通过 |
| 4 | 编译主程序 LoongBrowser.exe，实际启动验证 | 能打开网页、各功能菜单可用 |
| 5 | 编写自研安装器 Installer.cs 并编译 | 生成 LoongBrowserSetup.exe |
| 6 | 运行安装器 → 验证安装、快捷方式、默认浏览器注册、卸载 | 全流程可逆 |
| 7 | 交付：源码 + 计划书 + README + 安装包 | 文件齐全 |

---

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| 目标机器缺 WebView2 运行时 | 启动检测 + 明确提示下载微软官方 Evergreen 运行时（约 2MB 引导器） |
| Win10+ 强制经系统设置确认默认浏览器 | 程序自动完成注册表声明并跳转设置页，用户一次点击即可，属系统安全设计 |
| 无第三方打包工具（Inno Setup 未安装） | 自研 C# 安装器，功能覆盖文件复制/注册表/快捷方式/卸载，无外部依赖 |
| 系统自带 csc 仅支持 C# 5 语法 | 代码按 C# 5 标准编写（无字符串插值等新语法），编译时指定 `/codepage:65001` |
| 中文路径/编码问题 | 源码统一 UTF-8；安装路径默认 `%LocalAppData%\Programs\LoongBrowser`，用户可自定义，安装器校验路径合法性 |

---

## 六、交付物清单

1. `开发计划书.md`（本文件）
2. `src/` 全部 C# 源码
3. `build.ps1` 一键构建脚本
4. `LoongBrowserSetup.exe` —— 最终可安装的安装程序（位于 `dist\`）
5. `README.md` —— 使用与安装说明
