# 新标签页展示书签墙（含图标）—— 实现说明与边界清单

- 日期：2026-09-15
- 状态：**已实现并端到端验证通过**（真实 WebView2 内核）
- 版本：LoongBrowser v1.0.1 → 本次为功能新增

---

## 一、结论先行

| 行为 | 现状 |
|---|---|
| 「＋新标签」打开的新标签页 | **显示所有已收藏书签，每张卡片带图标** |
| 启动窗口（无命令行参数） | **仍为空白页**（沿用你之前"默认主页 = 空白页 about:blank"的设定）；`NewTabPage.UseForStartup = true` 可改为启动即显示书签墙 |
| 某站点拿不到图标 | **统一显示内置的空白图标**（`_default.png`，32×32）；旧的"首字母色块"可通过 `NewTabPage.UseLetterFallback = true` 切回 |
| 地址栏 | 新标签页期间保持空白（不显示 `about:blank`，也不显示任何本地文件路径） |

---

## 二、三种可选实现方式与选型

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| A. 生成本地文件页 | 把 HTML 写到 `%APPDATA%\LoongBrowser\newtab\index.html`，然后 `Navigate(file:///…)` | 图标可用相对路径、能被内核缓存 | **地址栏会暴露 `file:///C:/…` 路径**；跨源/本地资源受限；要处理文件写入与多窗口并发 |
| **B. `NavigateToString` 注入（已采用）** | 在内存里拼 HTML，直接注入 | 不落文件、地址栏干净、无 `file://` 限制、离线可用 | HTML 在内存中，需要主动控制体积 |
| C. 本地 HTTP 服务托管 | 起一个本地站点渲染 | 资源缓存最标准 | 引入端口占用、防火墙提示、额外攻击面，与"零依赖极简"目标冲突 |

选 B 的原因：零额外进程、零端口、离线可用，配合"图标归一化 + 数量上限"能把体积压住（实测 505 个书签 94 KB）。

---

## 三、关键模块与改动点

| 文件 | 类型 | 职责 | 关键符号 |
|---|---|---|---|
| `src/NewTabPage.cs` | 新增 | 生成书签墙 HTML：布局、URL 安全过滤、图片内联、点击桥脚本 | `BuildHtml` `IsSafeUrl` `IsInternalUrl` `RequestMissingIcons` `Enabled` `UseForStartup` `UseLetterFallback` `HomeUrl` `MaxCards` `MaxIcons` |
| `src/FaviconCache.cs` | 新增 | 图标三级来源、统一归一化为 32×32 PNG、缓存读写、并发去重 | `CaptureFromWebView` `FetchRemote` `GetCached` `ToDataUri` `DefaultIconFile` `MarkNoIcon` `Clear` `IconPath` `HostOf` |
| `src/TabManager.cs` | 修改 | 接线：空 url → 书签墙；采集图标；点击桥校验 | `Bookmarks` `ShowNewTabPage` `RefreshNewTabPages` `SetEnvironment`、`Wire()` 内新增 `NavigationStarting` / `FaviconChanged` / `WebMessageReceived` |
| `src/BookmarkStore.cs` | 修改 | 变更通知与查询 | `event Changed` `Remove` `Contains` |
| `src/MainForm.cs` | 修改 | 注入书签、书签变化刷新、启动页路由、地址栏规则、开关菜单 | `_bookmarks.Changed`、工具菜单「新标签页显示书签」 |

### 配置项一览（全部为静态字段，改一行即生效）

| 配置 | 默认值 | 含义 |
|---|---|---|
| `NewTabPage.Enabled` | `true` | 关掉即回到空白新标签页（工具菜单可切） |
| `NewTabPage.UseForStartup` | `false` | 启动窗口是否也显示书签墙 |
| `NewTabPage.UseLetterFallback` | `false` | 无图标时改用"首字母色块"而非空白图标 |
| `NewTabPage.HomeUrl` | `"about:blank"` | 启动页地址 |
| `NewTabPage.MaxCards` | `500` | 最多渲染多少张卡片 |
| `NewTabPage.MaxIcons` | `60` | 最多内联多少个真实图标 |
| `FaviconCache.EnableRemoteFetch` | `true` | 是否允许联网抓 `/favicon.ico` |
| `FaviconCache.RemoteTimeoutMs` | `6000` | 抓取超时 |
| `FaviconCache.IconSize` | `32` | 图标边长（显示与归一化尺寸） |
| `FaviconCache.MaxSourceBytes` | `512 KB` | 单个源图上限，超过丢弃 |
| 缓存目录 | `%APPDATA%\LoongBrowser\favicons` | 一个 host 一个 PNG；删目录即清缓存 |

---

## 四、布局实现

- 容器 `max-width:1080px` 居中；网格 `repeat(auto-fill, minmax(196px, 1fr))`，窄窗口自动降为单列。
- 卡片 = 32px 图标 + 标题（`-webkit-line-clamp:2` 两行截断，`word-break:break-all`）；`title` 属性给出完整"标题 + URL"。
- 有 hover 抬升 / 焦点描边（键盘 Tab 可达，回车即打开）。
- 空书签时显示引导页（"还没有书签 / 在任意页面点工具栏的「☆ 收藏」"）。
- 深色适配：`@media (prefers-color-scheme: dark)` 整套换色。
- 左上角显示"书签 N 个"（只统计能上屏的，与卡片数一致）。

**布局边界**：标题极长 → 截断 + tooltip；书签超过 `MaxCards` → 只渲染前 500 张并正常滚动；窗口极窄 → 单列；没有分页/搜索（超出 500 需靠书签管理对话框）。

---

## 五、图标来源：三级优先级（均已在真实内核上验证）

| 级别 | 通道 | 说明 | 实测结果 |
|---|---|---|---|
| ① | WebView2 官方：`FaviconChanged` → `GetFaviconAsync(Png)` | 拿到的就是**站点自己声明**的图标，最准 | 事件触发 1 次，`FaviconUri=https://cn.bing.com/sa/simg/favicon-trans-bg-blue-mg-png.png`，取图成功 526 字节 |
| ② | 兜底抓取 `https://<host>/favicon.ico` | HttpWebRequest，6 秒超时，显式开启 TLS 1.2，带浏览器 UA | bing / github / baidu 均 `200 image/x-icon`（ICO 魔数 `00 00 01 00`），归一化后 1233 字节 |
| ③ | 内置**空白图标** `_default.png` | 运行时生成 32×32 浅灰圆角方块，深浅背景下都可见 | `example.com/favicon.ico` 返回 **404** → 已把空白图标记在该 host 名下（32×32） |

**统一归一化**：PNG / JPEG / GIF / BMP / ICO 一律缩放居中到 **32×32 PNG**（ICO 走 `System.Drawing.Icon`，并对"指定尺寸失败"做了回退）。**SVG 图标不支持**（`System.Drawing` 无法栅格化）→ 落到空白图标。

### 两个必须知道的陷阱

1. **重定向会让图标"归属另一个域名"**。访问 `www.bing.com` 会被重定向到 `cn.bing.com`，官方通道于是把图标记在 `cn.bing.com` 名下；而书签里存的是 `www.bing.com` → 书签侧查不到。这正是第 ② 级兜底存在的意义（它会按**书签域名的 host** 再抓一次）。实测两条通道都产出成功。
2. **404 也要"记账"**。站点明确返回 404 时立即写入空白图标，否则每次打开新标签页都会重新发一次注定失败的请求。网络超时/不通则**不落盘**，留待下次重试（避免把临时故障永久记成"无图标"）。

---

## 六、性能与实测数据

| 项 | 数据 |
|---|---|
| 单个真实图标 | 约 1.2 KB，内联成 `data:URI` 后约 1.6 KB |
| 空白图标共享优化 | 505 个书签的页面 HTML：**416 KB → 94 KB**（空白图标用 CSS 背景全页只内联 1 份，而不是每张卡片复制一份） |
| 上限 | 卡片 500、内联真实图标 60、单个源图 512 KB、抓取超时 6 秒 |
| 并发 | 同一 host 同时只发一次请求（`_inFlight` 去重）；已有图标不覆盖，避免反复写盘 |
| 线程 | 抓取在 `ThreadPool`，完成后 marshal 回 UI 线程刷新新标签页（不卡界面） |
| 渲染 | 每次渲染要读盘 + base64 编码；500 个书签量级为几十毫秒，可接受 |

**性能边界**：书签越多，首次渲染越慢、HTML 越大（线性）；`MaxIcons` 之外的图标会退化为空白图标；`NavigateToString` 的内容过大（几百 KB 以上）会明显增加解析开销。若将来书签量级上千，建议改为"分页 + 只渲染可视区（虚拟滚动）"或方案 A（把图标放磁盘用相对路径）。

---

## 七、边界情况与安全清单

**安全（这是最容易出事的地方）**

- 书签 URL 可能是从地址栏输入来的，因此只放行 `http` / `https` / `file` 三种协议；`javascript:` / `data:` / `vbscript:` / `ftp:` **一律不上屏**（已测试）。这挡住了"新标签页变成 XSS 入口"。
- 标题与 URL 全部做 HTML 转义（`& < > " '` 与控制字符），避免属性被截断注入。
- 页面带 CSP：`default-src 'none'; img-src data:; style-src 'unsafe-inline'; script-src 'unsafe-inline'` —— 页面**无法发起任何外部请求**（图标必须是已内联的 data URI）。
- **点击桥双重校验**：只有"当前页面是内部页（`about:blank`）" **且** "目标 URL 确实在书签列表里"才导航。已端到端验证：页面 `postMessage` 一个未收藏的地址 → 宿主**不导航**。

**功能边界**

| 场景 | 行为 |
|---|---|
| `file://` 本地书签 | 可显示、可点击。点击走宿主 `Navigate`，绕开内核"禁止网页访问本地资源"的限制 |
| 没有书签 | 引导页，不报错 |
| `bookmarks.json` 损坏 / 缺失 | `JsonStore.Load` 静默返回 null → 书签列表为空 → 显示引导页，**不崩溃** |
| 书签/图标超量 | 双上限兜底（500 / 60） |
| 完全离线 | 页面与空白图标完全本地；已缓存图标照常显示；未缓存的显示空白图标 |
| 浅色/深色系统主题 | CSS 已适配；实际生效取决于内核上报的主题（如需强制可设 `CoreWebView2.Profile.PreferredColorScheme`） |
| 中键 / "在新窗口打开" | 卡片是普通链接，中键等走内核默认行为（`NewWindowRequested` → 新标签页） |
| 站点换了图标 | 已存在的图标**不会**被覆盖；需删除 `%APPDATA%\LoongBrowser\favicons` 或调用 `FaviconCache.Clear()`（目前未接菜单） |
| 隐私 | 兜底抓取会向每个书签域名各发 **1 次** `https://<host>/favicon.ico` 请求（只取图标）。不希望暴露书签域名可设 `FaviconCache.EnableRemoteFetch = false`，此时完全依赖"访问过的站点"与空白图标 |

---

## 八、验证结果

| 套件 | 结果 | 日志 |
|---|---|---|
| 新标签页单元测试（真实 `NewTabPage` / `FaviconCache`） | **42 / 42** | `tests/newtab_test_log.txt` |
| 新标签页端到端（真实 WebView2 + 真实 `TabManager` 接线） | **18 / 18** | `tests/newtab_e2e_log.txt` |
| 回归：`NormalizeUrl` | 25 / 25 | `tests/normalizeurl_log.txt` |
| 回归：双击打开本地 HTML | 5 / 5 | `tests/e2e_openfile_log.txt` |
| 编译 | BUILD OK | `build_log.txt` |

端到端覆盖的关键点：卡片渲染数量与标题、真实图标以 data URI 内联、空白图标由 CSS 背景提供、图标文件生成、**未收藏地址被拒绝**、**点击卡片成功跳转**、官方通道与兜底通道各自产出、404 站点落空白图标、归一化尺寸 32×32、地址保持内部页。

---

## 九、本次改动文件

- 新增：`src/NewTabPage.cs`、`src/FaviconCache.cs`
- 修改：`src/TabManager.cs`、`src/BookmarkStore.cs`、`src/MainForm.cs`
- 测试：`tests/NewTabPageTest.cs`、`tests/run_newtab_test.ps1`、`tests/NewTabE2ETest.cs`、`tests/run_newtab_e2e.ps1`、`tests/FaviconApiDump.cs`、`tests/run_favicon_dump.ps1`
