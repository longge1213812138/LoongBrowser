# 哔哩哔哩画中画跳空白页 —— 分析与修复

- 日期：2026-09-15
- 现象：B 站视频页点「画中画」跳转到**空白页**；其他网站的视频画中画正常
- 结论：**不是画中画功能坏了，而是当前标签被应用自己的弹窗处理导航成了空白页。**

---

## 一、一句话根因

B 站的画中画会**开一个小窗**（`window.open('about:blank', …)`），而应用把所有弹窗都接管了；
`about:blank` 又被同站判定当成"同站"，于是应用把**正在看的视频页**导航成了 `about:blank` —— 你看到的就是那张空白页。

---

## 二、B 站与其他站点在画中画机制上的差异

| 环节 | 其他网站的视频 | 哔哩哔哩 |
|---|---|---|
| 画中画实现 | 标准 `video.requestPictureInPicture()` | 标准 API + **自有"小窗"实现**（先 `window.open('about:blank', name, 'width=…,height=…')`，再往这个窗口里写入播放器） |
| 是否产生弹窗请求 | **不产生**（纯渲染器内部行为） | **产生** `NewWindowRequested`，且 `Uri = "about:blank"` |
| 是否经过应用的弹窗处理 | 不经过 | **经过** ← 出事的位置 |
| 结果 | 正常 | 当前标签被 `Navigate("about:blank")` → 空白页 |

### 两条支撑事实（本机实测）

1. **WebView2 SDK 完全没有画中画 API**。我用反射枚举了 `Microsoft.Web.WebView2.Core.dll` 的全部导出类型与成员，匹配 `Picture`/`Pip`/`Popup` 的类型为**空**。
   → 标准画中画全程在内核内部完成，宿主程序**不可能**把它弄坏。所以"能被弄坏的"只可能是站点自己开小窗那条路。
2. **真实内核里标准画中画是可用状态**：打开真实 B 站视频页实测 `document.pictureInPictureEnabled === true`，页面存在 `<video>`，播放器里有专用控件 `.bpx-player-ctrl-pip`（且播放中 `disabled=false`）。

---

## 三、根因（代码级）

修复前 `src/TabManager.cs` 的弹窗处理：

```csharp
view.CoreWebView2.NewWindowRequested += delegate(object s, CoreWebView2NewWindowRequestedEventArgs e)
{
    e.Handled = true;                     // ← 一律接管
    string target = e.Uri;                // ← B 站小窗这里等于 "about:blank"
    ...
    if (IsSameSite(current, target))      // ← IsSameSite 对 about: 协议直接 return true
        view.CoreWebView2.Navigate(target);// ← 于是把"当前标签"导航成了空白页
    else
        NewTab(target);
};
```

而 `IsSameSite` 里有这么一行（本意是"内部协议保守留在当前页"）：

```csharp
if (tu.Scheme == "about" || tu.Scheme == "javascript" || tu.Scheme == "data") return true;
```

`about:blank` 正好命中它 → 被判为"同站" → 走 `NavigateCurrent` 分支 → 视频页被顶掉。

### 实测复现（`tests/run_pip_popup_e2e.ps1` 阶段 A：原样搬来旧逻辑）

```
弹窗请求里的 Uri = about:blank
window.open 返回值 = null          ← 弹窗被吞掉
宿主页标题: PIP_HOST  →  about:blank   ← 复现用户看到的空白页
```

---

## 四、修复

把弹窗分流抽成纯函数 `TabManager.RoutePopup(currentUrl, targetUri)`，并新增 `IsNavigable`（只认 `http` / `https` / `file`）：

| 弹窗目标 | 处理 | 理由 |
|---|---|---|
| 真实地址 + 同站 | `NavigateCurrent`：留在当前标签内导航 | 原有行为，不变（历史可回溯） |
| 真实地址 + 跨站 | `OpenTab`：新开标签页 | 原有行为，不变 |
| `about:blank` / 空 / `javascript:` / `data:` / `blob:` | `Native`：`e.Handled = false` 交回内核开原生窗口 | 这类弹窗没有可用地址，内容由 opener 脚本写入；必须让 opener 拿到真正的窗口句柄 |

```csharp
switch (RoutePopup(current, e.Uri))
{
    case PopupAction.NavigateCurrent: e.Handled = true; view.CoreWebView2.Navigate(e.Uri); break;
    case PopupAction.OpenTab:         e.Handled = true; NewTab(e.Uri);                     break;
    default:                          e.Handled = false;                                   break;
}
```

### 修复后实测（同一套端到端测试，阶段 B：走真实 `TabManager`）

```
window.open 返回值 = opened      ← 弹出小窗被内核接受（拿到了真正的窗口）
当前页标题仍是「新标签页」        ← 当前页没有被顶掉
```

---

## 五、验证结果

| 套件 | 结果 | 日志 |
|---|---|---|
| 弹窗分流（单元，含"旧规则陷阱"对照） | **27 / 27** | `tests/popup_route_log.txt` |
| 画中画弹窗（端到端，真实内核 + 真实 `TabManager`） | **8 / 8** | `tests/pip_popup_e2e_log.txt` |
| 全量回归（7 个套件） | **135 / 135** | `tests/*_log.txt` |
| 编译 | BUILD OK | `build_log.txt` |

---

## 六、还需要你亲眼确认一次（30 秒，可选）

我无法自动完成"**可信点击**"这一环：实测合成点击（`element.click()`）触发不了 B 站的处理函数——点击后 `requestPictureInPicture` 未被调用、也没有 `window.open`，说明它的处理函数要求真实用户操作或可信事件；而 SDK 里的 `SendMouseInput` 只存在于**合成模式**（`CoreWebView2CompositionController`），本项目用的是标准窗口模式，用不了。用真实鼠标注入（SendInput）会占用你正在用的桌面，我没有擅自做。

所以"B 站到底走哪条路径"，请你在新版里按下面看一眼即可确认：

1. 打开 B 站任意视频 → 按 **F12** 打开开发者工具（WebView2 默认允许）→ 切到 Console
2. 粘贴并回车：

```js
window.__o=[]; var r=window.open;
window.open=function(u,n,f){ __o.push(String(u)+' | '+n); return r.apply(window,arguments); };
window.__p=[]; var q=HTMLVideoElement.prototype.requestPictureInPicture;
HTMLVideoElement.prototype.requestPictureInPicture=function(){
  __p.push('called'); var x=q.apply(this,arguments);
  if(x&&x.then) x.then(function(){__p.push('ok')}, function(e){__p.push('rejected:'+e.name)});
  return x;
};
```

3. 点播放器上的画中画按钮，然后查看 `__o` 与 `__p`：

| 观察结果 | 含义 | 对应处理 |
|---|---|---|
| `__o` 里出现 `about:blank \| …` | 走的是**小窗**路径 | 本次修复正是针对它，应该已好 |
| `__p` 显示 `rejected:NotAllowedError` 等 | 标准 API 在 WebView2 里被拒，站点转走兜底小窗 | 同一条修复路径，已覆盖 |
| `__p` 显示 `ok` 且确实进入了画中画 | 那就是另一条路（WebView2 内部创建画中画窗口） | 把结果发我，我沿"画中画窗口创建是否产生新窗口请求"继续查 |

---

## 七、后续排查方向

1. **其他弹窗一起看**：OAuth 授权、支付、打印预览都走同一条 `NewWindowRequested`，本次修复一并覆盖；若还有"点某按钮跳空白页"，先怀疑同一处。
2. **小窗弹出但内容空白**：那属于小窗自身渲染问题（与本次修复无关），可先用 `--disable-features` 之类参数对照排除。
3. **想更宽容**：若希望彻底"像浏览器一样"放开弹窗，把 `RoutePopup` 的 `Native` 分支改成对所有地址都返回 `Native` 即可（代价是 `target="_blank"` 链接不再变成标签页，而是独立窗口）。
4. **站点兼容层**：若某些站点在 `document.pictureInPictureEnabled` 为 false 时表现异常，可用 `CoreWebView2.AddScriptToExecuteOnDocumentCreatedAsync` 在文档创建时注入兼容脚本。

---

## 八、本次改动文件

- 修改：`src/TabManager.cs`（`PopupAction` / `RoutePopup` / `IsNavigable`，`NewWindowRequested` 处理）
- 新增测试：`tests/PopupRouteTest.cs` + `tests/run_popup_route_test.ps1`、`tests/PipPopupE2ETest.cs` + `tests/run_pip_popup_e2e.ps1`
- 文档：`README.md` 测试章节增补两套用例

---

## 九、后续：小窗置顶（新增功能）

### 为什么必须自建窗口

第一版修复是 `e.Handled = false` **交回内核**开原生弹窗——这能止血（不再顶掉页面），但内核开的窗口**不归应用管**，拿不到窗口句柄，也就无法做"始终在最前"。

所以改为**自建窗口**：`NewWindowRequested` 里自己开一个 `PopupWindow`（内含独立 WebView2），用 `e.GetDeferral()` 等内核初始化完成，再 `e.NewWindow = 该窗口的内核` + `e.Handled = true` 交回发起方。
这样 opener 拿到的仍是**真正的 window 句柄**（B 站小窗要往里写播放器），而窗口控制权在我们手里。

### 实现要点（`src/PopupWindow.cs`）

| 能力 | 做法 |
|---|---|
| 窗口置顶 | `Form.TopMost = true`；窗口顶部 30px 工具条上有「窗口置顶」复选框，勾选状态即时生效 |
| 可开可关 | 复选框 ↔ `SetAlwaysOnTop(bool)`；状态写入 `%APPDATA%\LoongBrowser\popup.json`（`DefaultAlwaysOnTop`），下次开小窗沿用 |
| 默认值入口 | 主窗口「工具 → 小窗默认置顶」；也可改代码里的 `PopupWindow.DefaultAlwaysOnTop` |
| 尺寸 | 按站点请求的 `WindowFeatures.Width/Height` 设置内容区，再加工具条高度（不合理的小值会被忽略） |
| `window.close()` 联动 | 监听 `CoreWebView2.WindowCloseRequested` → 关闭宿主窗口，避免留下空白孤儿窗口 |
| 兜底 | 建窗/初始化任何一步失败 → `e.Handled = false`，退回"让内核开原生窗口"，不至于把弹窗彻底挡掉 |

### 验证（`tests/run_popup_window_e2e.ps1`，真实内核 + 真实 `TabManager`，12 项）

```
A1 window.open 拿到了窗口（不再是 null）
A2 小窗已由我们自己创建
A3 小窗尺寸: 420×290（内容区 260 + 工具条 30）
A4 小窗默认置顶（始终显示在最前）
B1 opener 可写入并读回小窗文档      ← B 站小窗能工作的前提
C1/C3 关闭置顶 → TopMost=false；重新开启 → TopMost=true
C2/C4 开关状态已记住（写入 popup.json）
D1 页面调 window.close() 后宿主窗口跟着关闭（无孤儿窗口）
```

### 已知边界

- 置顶是**窗口级**的，对"独占全屏"的程序（部分游戏、全屏视频播放器）无效——这是 Windows 本身的限制，任何 TopMost 窗口都会这样。
- 小窗与主窗口同属一个进程与内核环境，主窗口关闭退出时小窗一并结束。
- OAuth 授权窗走的是同一条路径，因此也会默认置顶；不需要时在窗口里取消勾选（或用工具菜单改默认值），选择会被记住。
