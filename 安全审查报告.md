# LoongBrowser 安全审查报告

- **审查日期**：2026-09-16
- **审查范围**：`src/*.cs`（14 个源文件）、`build.ps1`、`.gitignore`、Release 发布形态
- **版本**：v1.1.0（WebView2 SDK 1.0.4191.47）
- **方法**：人工代码审计（威胁建模 + 逐模块走查），未做动态渗透测试
- **结论概览**：**2 项高危、5 项中危、4 项低危、3 项提示**。整体安全基线好于同规模个人项目（CSP 严格、URL 白名单、消息桥校验到位），但**安装器的卸载路径处理**存在真实的数据损毁风险，建议优先修。
- **2026-09-16 更新**：H2（本地数据明文）已修复 —— `history.json` 改用 DPAPI 加密、内核数据目录启用 EFS 标记，30 项加密回归测试全通过。剩余高危仅 H1。

---

## 一、高危

### H1 卸载可递归删除用户输入的任意目录（数据损毁）

**位置**：`src/Installer.cs:366`（卸载按钮）、`DoUninstall` → `src/Installer.cs:178-180`

```csharp
btnUninstall.Click += delegate {
    if (Program.DoUninstall(true, _dirBox.Text.Trim())) Close();   // ← 用文本框内容当安装目录
};
...
string dir = !string.IsNullOrEmpty(customDir) ? customDir : GetInstalledDir();
if (!string.IsNullOrEmpty(dir) && Directory.Exists(dir)) TryDeleteDir(dir);   // Directory.Delete(dir, true)
```

**问题**：
1. 卸载目标是**界面文本框里的当前内容**，不是注册表记录的 `InstallLocation`。用户点过"浏览…"选了别的目录（比如 `D:\资料`、某个项目目录）之后再点"卸载"，该目录会被**整棵递归删除**。
2. 卸载路径**没有复用安装时的合法性校验**。`DoInstall` 挡掉了 `C:\` 和 Windows 目录（`Installer.cs:57-62`），`DoUninstall` 完全没有。
3. 确认弹窗只说"书签等用户数据将被一并删除"，没有提示会删除哪个目录，用户无从判断。
4. `TryDeleteDir` 静默吞异常，删到一半失败也不告知。

**影响**：不可逆的用户数据丢失。

**修复建议**：
```csharp
// 1) 卸载一律以注册表记录为准，UI 只做展示
string dir = GetInstalledDir();

// 2) 复用安装时的路径校验（抽成 IsForbiddenDir(targetDir)），
//    额外补充：必须是已存在目录、且目录下确实有 LoongBrowser.exe 才允许删
// 3) 确认框里显示将要删除的完整路径
// 4) 删除前先改名为 *.loongbrowser.bak，成功后再清理，便于找回
```

---

### H2 Cookie / 登录态与浏览历史明文落盘，无加密保护 —— **已修复（2026-09-16）**

**位置**：`src/JsonStore.cs:14-20`（`UserDataDir`）、`src/HistoryStore.cs:25`

- WebView2 用户数据目录（**Cookie、LocalStorage、IndexedDB、缓存**）固定在
  `%LOCALAPPDATA%\LoongBrowser\WebView2`，**未做任何加密**。
- 现代浏览器（Chrome / Edge）用 **DPAPI** 加密 Cookie 库，正是为了防止"任意同权限进程直接拷走会话"。当前实现下，任何以该用户运行的脚本或程序都能直接复制整个目录，拿到全部站点登录态。
- `history.json` 明文记录完整 URL，**包含查询串**（可能带 token、session 参数、搜索关键词），最大 3000 条长期留存。

**已实施的修复（仅加密，未新增任何功能/开关）**：

| 资产 | 措施 | 位置 |
|---|---|---|
| `history.json` | **DPAPI（`CurrentUser` 作用域）加密落盘**，带 `LBE1` 魔数前缀 + 应用专属熵 | `JsonStore.SaveProtected` / `LoadProtected` |
| 内核数据目录（Cookie 等） | 创建后打上 **EFS 加密标记**，之后写入的文件由文件系统自动加密 | `AppPaths.EnsureUserDataDirEncrypted()`，由 `TabManager.NewTab` 在建环境前调用 |

**兼容与降级（都已处理，无感）**：
- 升级前遗留的**明文** `history.json` 仍可读（按魔数区分格式），下次保存自动转为密文 —— 老用户不会"一升级历史就没了"。
- 文件损坏 / 换系统用户导致解密失败 → 返回空列表，**不抛异常、不崩溃**。
- DPAPI 不可用时回退明文写入，宁可弱保护也不丢数据。
- EFS 在家庭版 / 非 NTFS 上不支持 → 静默跳过，不影响启动与浏览（**本机实测即走这条分支**，见下）。

**实测边界（必须知道）**：
- 本机（Windows 家庭版或非 NTFS）**EFS 不可用**，`EnsureUserDataDirEncrypted` 返回 false 并静默跳过。也就是说，**history.json 的 DPAPI 加密是确定生效的主保护；Cookie 目录加密是"尽力而为"**，在不支持 EFS 的机器上仍靠用户账户 ACL 保护。
- EFS 标记只影响**之后新建**的文件，不回溯加密已有缓存（内核正占用，强行递归会失败且拖慢启动）。首次安装时目录为空，效果最佳。

**回归测试**：`tests/run_encrypted_store_test.ps1` → **30 项全通过**，含"磁盘上读不到明文网址/token/中文标题"、"旧明文文件兼容"、"损坏文件不崩"、"真实 HistoryStore 端到端"；`tests/run_history_test.ps1`（27 项）、`tests/run_history_e2e.ps1`（真实内核 13 项）均通过。

**未采纳的建议**（按"只做加密、不加功能"的要求）：无痕/隐私模式、退出时自动清理 Cookie、历史 URL 查询串清洗、历史"不记录"开关。如需再补，可另开一轮。

---

## 二、中危

### M1 单实例 URL 转发文件无保护，可被本地进程强制导航

**位置**：`src/Program.cs:63-64`（写）、`src/MainForm.cs:72-91`（轮询认领 + 导航）

`%APPDATA%\LoongBrowser\open_url.txt` 位于普通用户目录，**无 ACL 收紧、无完整性校验、无来源校验**；主窗口每 800ms 轮询一次，读到内容就直接 `NewTab(url)` 导航，且 **file:// 也放行**。

**影响**：任何同权限进程（包括恶意脚本）可在用户不知情时让浏览器打开任意地址 —— 结合已保存的 Cookie，可当作 CSRF 的放大器；也可强制打开本地 HTML 做钓鱼。

**修复建议**：
- 创建文件时设置 ACL（仅当前用户 SID 可写），或改用**命名管道 / WM_COPYDATA** 并在接收端校验对端进程路径。
- 接收端再过一次白名单（`TabManager.IsNavigable`），并对 `file://` 单独提示。
- 轮询间隔 800ms 的 TOCTOU 可用文件锁缓解。

### M2 图标解码存在"压缩炸弹"与 GDI+ 解析攻击面

**位置**：`src/FaviconCache.cs:297-353`（`Normalize` / `LoadImage`）

- `MaxSourceBytes = 512KB` 只限制**输入体积**，挡不住解压炸弹：几百 KB 的 PNG/GIF 可解出数 GB 位图 → 内存耗尽（DoS）。
- 使用 `System.Drawing`（GDI+）解码**完全不可信**的远程图片。GDI+ 历史上多次出现内存破坏类漏洞，而这类崩溃**无法被 try/catch 兜住**（现有代码正是靠 catch 兜底）。

**修复建议**：
- 先用 `Image.FromStream(s, false, false)` 只读头部，校验 `src.Width/Height ≤ 4096` 且像素总量上限，通过后再全量解码。
- 优先改用 WPF 的 `BitmapSource`（WIC）或托管解码库，降低 GDI+ 攻击面。
- 严格来说还应限制并发解码数（`ThreadPool.QueueUserWorkItem` 目前无上限，60 个书签可同时起 60 个解码）。

### M3 同站判断把公共后缀当根域（`*.com.cn` 全部视为同站）

**位置**：`src/TabManager.cs:478-485`

```csharp
string[] parts = host.Split('.');
if (parts.Length <= 2) return host;
return parts[parts.Length - 2] + "." + parts[parts.Length - 1];   // a.com.cn 与 b.com.cn → 都是 com.cn
```

**影响**：`evil.com.cn` 与 `bank.com.cn` 会被判为"同站"，`target="_blank"` 的跨站链接会在**当前标签内导航**，顶掉用户正在看的页面并污染后退历史 —— 可用于 UI 欺骗/钓鱼。（同源策略不受影响，所以不是直接的跨域漏洞，但属于真实的判断过宽。）

**修复建议**：引入公共后缀列表（PSL），或至少对 `com.cn/net.cn/org.cn/gov.cn/edu.cn/co.uk/jp` 等常见二级后缀做例外，取三段。

### M4 构建依赖未入库、无哈希校验（供应链）

**位置**：`.gitignore:9`（`libs/`）

`libs/` 下 3 个 WebView2 SDK DLL 被 gitignore，仓库里**没有依赖，也没有版本/哈希声明**。

**影响**：换机/换人构建时依赖来源不可追溯、不可校验；若 DLL 被替换，构建产物会被静默植入后门。

**修复建议**：在仓库里保留 `libs/README.md` 记录**版本号 + SHA256**，或改用 NuGet 还原 + 锁文件（推荐），并在 `build.ps1` 里校验哈希后再编译。

### M5 程序安装到用户可写目录，二进制可被同权进程替换

**位置**：`src/Installer.cs:22-29`（默认 `%LocalAppData%\Programs\LoongBrowser`）

安装目录对当前用户可写，同权限进程可替换 `LoongBrowser.exe` 或 **`WebView2Loader.dll`**（典型的 DLL 侧加载/持久化位置）。卸载项的 `UninstallString` 也指向该目录，替换后可在卸载时被触发执行。

**修复建议**：提供"安装到 Program Files（需管理员）"选项并设为推荐；或对自身文件做完整性校验（启动时比对哈希/签名）。

---

## 三、低危

| # | 问题 | 位置 | 说明与建议 |
|---|---|---|---|
| L1 | 右键"在新标签页打开链接"未过滤协议 | `TabManager.cs:352` | `NewTab(target.LinkUri)` 直接用，网页可提供 `javascript:` / `data:` / `file:` 链接。建议把协议白名单收敛到 `NewTab` 入口一处统一过滤（`IsNavigable`）。 |
| L2 | favicon 抓取重定向无限制（SSRF 面） | `FaviconCache.cs:277-292` | `AllowAutoRedirect=true` 且不限跳数/目标，可被引导至内网或云元数据地址（响应不回显，仅可探测）。建议限制 1 跳、拒绝非 https 目标与私有地址段。 |
| L3 | 全局 Mutex 无 ACL + 按进程名激活窗口 | `Program.cs:54`、`93` | 其他会话用户可抢占 `LoongBrowser_SingleInstance_Mutex` 造成启动失败（DoS）；`Process.GetProcessesByName("LoongBrowser")` 可能激活伪装的同名进程。建议 Mutex 名加 `Local\` 前缀并设 ACL，激活改用 IPC/窗口消息。 |
| L4 | `/uninstall` 静默参数实际会弹确认框 | `Installer.cs:37` | `DoUninstall(true, null)` 传了 `interactive=true`，与"静默卸载"语义相反，批量部署会卡住。应传 `false` 或增加独立开关。 |

---

## 四、提示（非漏洞，但建议改进）

- **P1 发布物未签名、未提供校验值**：`LoongBrowser-v1.1.0-win64.zip` / `Setup.exe` 无数字签名，用户侧无法校验完整性。建议签名，至少在 Release 页面附 SHA256。
- **P2 无应用程序清单**：`build.ps1` 未指定 `/win32manifest`，缺少 DPI 感知与执行级别声明。建议加 `app.manifest`（`asInvoker` + `dpiAware`）。
- **P3 WebView2 SDK 版本**：当前 `1.0.4191.47`。内核走 Evergreen 会自动更新，风险主要在 SDK/API 层；建议核对是否为当前最新并定期升级。
- **P4** `NormalizeUrl` 对 `localhost:8080`（无协议）会原样返回并交给内核，行为不确定，建议显式补全 `http://`。

---

## 五、做得好的部分（无需改动）

- **新标签页 CSP 严格**：`default-src 'none'` + `img-src data:` + `base-uri/form-action 'none'`，即使有 HTML 注入也难以外泄或跳转（`NewTabPage.cs:66`）。
- **HTML 转义完整**：`Esc()` 覆盖 `& < > " '` 与控制字符，标题/URL 均转义后才拼进属性与文本（`NewTabPage.cs:159-175`）。
- **消息桥校验到位**：只接受内部页（`about:blank`）来源，且目标 URL 必须在书签列表内，避免网页随意驱动宿主导航（`TabManager.cs:318-328`）。
- **URL 协议白名单**：`IsNavigable` / `IsSafeUrl` 统一只放行 http/https/file，`javascript:`/`data:`/`blob:` 在弹窗路由前先被过滤 —— 这是画中画修复的关键，也顺带挡掉了一类注入。
- **反序列化无 RCE 面**：`JavaScriptSerializer` 仅反序列化简单 POCO，未使用 `SimpleTypeResolver`，不存在类型绑定 gadget。
- **注册表只写 HKCU**：安装与默认浏览器登记都不提权，符合最小权限。
- **TLS 1.2 显式协商**、**下载体积上限 512KB**、**历史条数上限 3000** 等资源边界都有考虑。

---

## 六、建议的修复顺序

1. ~~**H2**（数据加密）~~ → **已完成**（见上文，DPAPI + EFS，仅加密、未加功能）。
2. **H1**（卸载路径）—— 一行改动 + 复用校验，收益最高，剩余高危里最该先修。
3. **M1 / M2 / M3** —— 均可在 1~2 小时内完成。
4. **M4 / M5 / L1~L4 / P1~P2** —— 作为 v1.2 的加固清单整体处理。

---

*本报告基于静态代码审计。建议在修复后补充一轮针对 H1（卸载路径）与 M1（URL 转发）的针对性回归测试 —— 项目已有 `tests/run_*.ps1` 测试骨架，可直接复用。*
